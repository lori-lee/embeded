#include "config.h"
extern sys_status data g_sysstatus;
void check_do (void)
{
    if (!g_sysstatus.mode) {
        if (g_sysstatus.h_sensor) {
            if (g_sysstatus.l_sensor)
        } else {
        }
    }
}
